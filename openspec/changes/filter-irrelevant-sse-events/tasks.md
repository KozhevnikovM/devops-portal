## 1. Publish side: routing metadata

- [ ] 1.1 In `app/infrastructure/events.py`, add the frozen `RowRef(environment_id, owner_id, created_by)` value and extend `_payload` to serialise `owner_id` and `created_by` (always present, `created_by` may be null). Verify with a unit test that the payload has exactly the keys `booking_id`, `environment_id`, `kind`, `owner_id`, `created_by`.
- [ ] 1.2 Make `owner_id` / `created_by` required keyword arguments of `publish_row_changed`, `publish_progress_changed` and `apublish_row_changed`, and have `ProgressCoalescer.submit` / its trailing publish carry an opaque `RowRef` (latest wins) instead of the bare `environment_id`. Update `tests/test_progress_coalescing.py` for the new callable signature, and verify the whole file passes unchanged in behaviour, including a test that the trailing publish carries the latest `RowRef`.
- [ ] 1.3 Pass `user_id` / `created_by` from the ORM model at every publish call site in `app/infrastructure/repositories/booking_repo.py` (`update_status`, `extend`, `update_label`, `promote_next_queued`, `sync_update_status`, `sync_set_status_message`, `sync_record_progress`, `sync_promote_next_queued`). Verify by extending `tests/test_sse_row_changed.py` so each publish assertion includes the owner and creator ids, and by running mypy through `py-review` with no missing-argument errors.

## 2. Subscribe side: pre-filter before any DB lookup

- [ ] 2.1 In `app/presentation/routes/events.py`, add `_may_concern(payload, user)`. It returns `True` when `owner_id` is absent (legacy), and otherwise returns `can_manage(owner_id, created_by, user)`. Call it in `_event_stream` before `_rows_to_refresh`, and skip the message when it returns `False`. Add a comment noting the environment/child shared-ownership invariant. Verify with parametrised unit tests of `_may_concern` (owner, creating dispatcher, other dispatcher, unrelated user, admin, legacy payload).
- [ ] 2.2 Add `_event_stream` tests (using the existing `_FakePubSub` / `_drain_one_message` helpers) showing that an event for an unrelated user's booking, both `progress` and `lifecycle` with an environment id, results in no `AsyncSessionLocal` call, no booking or environment repository call and no chunk. Verify by patching `AsyncSessionLocal`, `_booking_repo` and `_env_repo` with mocks and asserting none were called.
- [ ] 2.3 Add `_event_stream` tests showing that the owner, the creating dispatcher and an admin still get the booking row (and the environment row for lifecycle), and that a legacy payload without `owner_id` still reaches the renderers and is authorized by the DB check. Make `_drain_one_message` take the connection's user. Verify that the existing #388 and #441 tests in `tests/test_events_stream.py` still pass.

## 3. Docs & verification

- [ ] 3.1 Update `docs/api-reference.md` (`GET /events/stream`) to say that events for rows the user cannot manage are filtered server-side before any lookup, and that the payload carries only ids. Verify by reading the rendered section.
- [ ] 3.2 Run `pytest tests/ -m "not integration"` and the `py-review` skill on the changed Python files. Verify both are clean.
- [ ] 3.3 Runtime check: with `docker compose up`, open the bookings page as two different ordinary users and as an admin, trigger a booking for user A, and confirm that A's and the admin's rows update live while B's stream receives no event for it (browser devtools EventStream tab). Also confirm from the app logs or a temporary debug counter that B's connection opened no session for it. If Docker is unavailable, say so and ask the user to perform this check.
