## 1. Publish side: routing metadata

- [x] 1.1 In `app/infrastructure/events.py`, add the frozen `Routing(owner_id, created_by)` value and extend `_payload` to serialise `owner_id` / `created_by` always and `environment_owner_id` / `environment_created_by` when environment routing is given. Verify with unit tests that a standalone payload has exactly the keys `booking_id`, `environment_id`, `kind`, `owner_id`, `created_by`, and that an environment-child lifecycle payload additionally has exactly the two environment keys.
- [x] 1.2 Make `booking_routing` a required argument of `publish_row_changed`, `publish_progress_changed` and `apublish_row_changed`, and add an optional `environment_routing` to the two lifecycle publishers. Have `ProgressCoalescer.submit` / its trailing publish carry an opaque `(environment_id, booking_routing)` context (latest wins) instead of the bare `environment_id`. Update `tests/test_progress_coalescing.py` for the new callable signature, and verify the whole file passes unchanged in behaviour, including a test that the trailing publish carries the latest context.
- [x] 1.3 Add an async and a sync repository helper that read `(user_id, created_by)` of an environment by primary key and return `Routing | None` (`None` when it no longer exists). Verify with unit tests for both the found and the missing case.
- [x] 1.4 Pass `Routing(model.user_id, model.created_by)` at every publish call site in `app/infrastructure/repositories/booking_repo.py` (`update_status`, `extend`, `update_label`, `promote_next_queued`, `sync_update_status`, `sync_set_status_message`, `sync_record_progress`, `sync_promote_next_queued`). At the lifecycle sites, when `environment_id` is set, also pass the environment routing from 1.3, and keep the lookup inside the best-effort publish so that a lookup error is logged and never fails the committed write. Verify by extending `tests/test_sse_row_changed.py`: each publish assertion includes the booking routing; lifecycle publishes for an environment child include environment routing while `sync_record_progress` never performs the environment lookup; and a raising environment lookup still leaves the write committed. Also run mypy through `py-review` and confirm there are no missing-argument errors.
- [x] 1.5 Regression test for the adoption case: a namespace booking with `created_by=None` (owner U) adopted into an environment with `created_by=D`. A lifecycle publish for it carries booking routing `(U, None)` and environment routing `(U, D)`. Verify in `tests/test_sse_row_changed.py`.

- [x] 1.6 (PR #463 review) Capture the booking's id, environment id and routing before the environment lookup, and run the lookup in its own short-lived session on the caller's engine, closed before the publish. The caller's session is never rolled back or left in a transaction. Verify with unit tests that simulate expiry (reading the booking after the lookup starts fails the test) and with Postgres integration tests (`tests/integration/test_row_changed_routing.py`, real mapped `BookingModel` and real sessions, async and sync, lookup succeeding and lookup failing with a real DB error). After the call, the caller's session must have no open transaction and no expired attributes. Confirm those tests fail against the pre-fix code.
## 2. Subscribe side: per-row pre-filter before any DB lookup

- [x] 2.1 In `app/presentation/routes/events.py`, add `_may_concern(payload, row, user)`. It reads the routing keys for that row (`owner_id`/`created_by` for booking, `environment_owner_id`/`environment_created_by` for environment), returns `True` when that row's owner key is absent (legacy), and otherwise returns `can_manage(...)`. In `_event_stream`, call it for each `(row, row_id)` from `_rows_to_refresh` and skip the renderer when it returns `False`. Verify with parametrised unit tests of `_may_concern` (owner, creating dispatcher, other dispatcher, unrelated user, admin, legacy booking row, legacy environment row).
- [x] 2.2 Add `_event_stream` tests (using the existing `_FakePubSub` / `_drain_one_message` helpers) showing that an event for an unrelated user's booking, both `progress` and `lifecycle` with an environment id and environment routing, results in no `AsyncSessionLocal` call, no booking or environment repository call and no chunk. Verify by patching `AsyncSessionLocal`, `_booking_repo` and `_env_repo` with mocks and asserting none were called.
- [x] 2.3 Add an `_event_stream` regression test for the PR #462 adoption case. For a connection belonging to dispatcher D and a lifecycle payload with booking routing `(U, None)` and environment routing `(U, D)`, the booking renderer is not called and the environment renderer is called and its chunk is yielded. Verify the test fails if the pre-filter uses the booking routing for the environment row.
- [x] 2.4 Add `_event_stream` tests showing that the owner, the creating dispatcher and an admin still get the booking row (and the environment row for lifecycle). Also show that a legacy payload without routing keys, and a lifecycle payload with booking routing but no environment routing, still reach the respective renderers and are authorized by the DB check. Make `_drain_one_message` take the connection's user. Verify that the existing #388 and #441 tests in `tests/test_events_stream.py` still pass.

## 3. Docs & verification

- [x] 3.1 Update `docs/api-reference.md` (`GET /events/stream`) to say that rows the user cannot manage are filtered server-side, per row, before any lookup, and that the payload carries only ids. Verify by reading the rendered section.
- [x] 3.2 Run `pytest tests/ -m "not integration"` and the `py-review` skill on the changed Python files. Verify both are clean.
- [x] 3.3 Runtime check: with `docker compose up`, open the bookings/environments pages as two different ordinary users, a dispatcher and an admin. Trigger a booking for user A, and a dispatcher-ordered environment for A that adopts A's existing standalone namespace. Confirm that A's, the admin's and (for the environment row) the dispatcher's rows update live, while B's stream receives no event (browser devtools EventStream tab). Also confirm from the app logs or a temporary debug counter that B's connection opened no session for them. If Docker is unavailable, say so and ask the user to perform this check.
